# Optional local voice pipeline (ASR + TTS). Install on top of requirements.txt
# only when running with SPEECH_ENABLED=true:
#   pip install -r requirements.txt -r requirements-speech.txt
#
# These are heavy / platform-specific and intentionally excluded from the base
# (Railway) image, which falls back to the browser Web Speech API.

faster-whisper==1.1.0
piper-tts==1.2.0
numpy==2.2.1
pydub==0.25.1
